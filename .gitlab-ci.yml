stages:
    - build
    - babel
    - deploy

build:
    stage: build
    script:
     - cd gui && make
    artifacts:
      paths:
        - gui/src/*.css
        - gui/src/js
        - gui/src/*.html
        - gui/src/*.svg
        - gui/src/*.png

build-babel:
    stage: build
    script:
    - cd babel && make

babel:
    stage: babel
    dependencies:
    - build
    script:
    - docker run -t --rm -v `pwd`:/work -w /work -u $(id -u):$(id -g) babel
    - mv gui/src/js/cytoutils.babel.js gui/src/js/cytoutils.js
    artifacts:
      paths:
        - gui/src/*.css
        - gui/src/js
        - gui/src/*.html
        - gui/src/*.svg
        - gui/src/*.png

pages:
  stage: deploy
  dependencies:
     - babel
  script:
       - mkdir public
       - mv gui/src/*.css public/
       - mv gui/src/js public/
       - mv gui/src/*.html public/
       - mv gui/src/*.svg public/
       - mv gui/src/*.png public/
       - mv public/graphTool.html public/index.html
       - rm -rf public/graphTool.html
  artifacts:
        when: on_success
        paths:
        - public
  only:
  - master
